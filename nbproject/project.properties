application.args=
application.title=hl7inspector
application.vendor=carsten.rambow
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/hl7inspector.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
jar.compress=true
javac.classpath=\
    ${libs.looks-2.1.4.classpath}:\
    ${libs.forms-1.1.0.classpath}:\
    ${libs.l2fprod-common-7.3.classpath}:\
    ${libs.apache-log4j-1.2.15.classpath}:\
    ${libs.simple-xml-1.6.2.classpath}:\
    ${libs.nanoxml-lite-2.2.3.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.deprecation=true
javac.source=1.5
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.apache-log4j-1.2.15.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.apache-log4j-1.2.15.classpath=../lib/apache-log4j-1.2.15/log4j-1.2.15.jar
# Property libs.forms-1.1.0.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.forms-1.1.0.classpath=../lib/forms-1.1.0/forms-1.1.0.jar
# Property libs.l2fprod-common-7.3.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.l2fprod-common-7.3.classpath=../lib/l2fprod-common-7.3/lib/l2fprod-common-all.jar
# Property libs.looks-2.1.4.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.looks-2.1.4.classpath=../lib/looks-2.1.4/looks-2.1.4.jar
# Property libs.nanoxml-lite-2.2.3.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.nanoxml-lite-2.2.3.classpath=../lib/nanoxml-lite-2.2.3/lib/nanoxml-lite-2.2.3.jar
# Property libs.simple-xml-1.6.2.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.simple-xml-1.6.2.classpath=../lib/simple-xml-1.6.2/jar/simple-xml-1.6.2.jar
main.class=de.elomagic.hl7inspector.Hl7Inspector
manifest.file=manifest.mf
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Djava.net.useSystemProxies=true
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
